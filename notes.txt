dueling.pth  849626 frames, does not work
dueling2.pth learn rate /= 10, 833577 additional frames

todo:
        * ht tuning (lr, scheduler)
        * learn state representation
        * wtf is wrong with ac
        * data, plots, whatever (should know already but fu)
        * ICM
        * importance sampling for memory batches
